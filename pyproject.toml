[project]
name = "sundars96-github-io"
version = "0.1.0"
description = ""
authors = [
    {name = "Sundar",email = "sundar96@bu.edu"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
]

[tool.poetry]
packages = [{include = "sundars96"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
